pkgname=joshuaxw-base
pkgver=0.0.1
pkgrel=1
pkgdesc="Base system packages for joshuaxw"
arch=('any')
# url=
license=('MIT')

depends=(
    # Base packages
    base linux linux-firmware man-db man-pages texinfo

    # Bootloader and related packages
    grub efibootmgr dosfstools os-prober mtools

    # Basic utilities
    networkmanager vim emacs firefox

    # base-devel group
    autoconf automake binutils bison fakeroot file findutils flex gawk gcc gettext grep groff gzip libtool m4 make pacman patch pkgconf sed sudo texinfo which

)

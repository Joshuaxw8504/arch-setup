pkgname=joshuaxw-desktop
pkgver=0.0.1
pkgrel=1
pkgdesc="Desktop packages for joshuaxw"
arch=('any')
# url=
license=('MIT')
install=joshuaxw-desktop.install

depends=(
    joshuaxw-base

    # Other applications
    # discord-canary

    # Game development
    jack2 qjackctl

    # VMs
    qemu libvirt virt-manager edk2-ovmf

    # Fun
    xonotic
)

source=('emacs.desktop')
md5sums=('SKIP')
package() {
    install -Dm 0644 emacs.desktop "$pkgdir/usr/share/xsessions/emacs.desktop"
}

